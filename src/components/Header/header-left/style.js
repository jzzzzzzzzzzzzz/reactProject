import { themeColor } from '@/assets/theme'
import styled from 'styled-components'
export const HleftWrapper = styled.div`
        flex: 1;
        .log {
                display: inline-block;
                cursor: pointer;
                color:${themeColor.primayColor}
        }
`