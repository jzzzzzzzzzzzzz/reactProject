import { themeColor } from '@/assets/theme'
import styled from 'styled-components'
export const HeaderWrapper = styled.div`
    display: flex;
    justify-content: space-between;
    height: 80px;
    border-bottom: 1px solid #eee;
    align-items: center;
    padding: 0px 30px;
    color:#222222;
    font-weight: 600;
    font-size: ${themeColor.mainFontSize};

   
`