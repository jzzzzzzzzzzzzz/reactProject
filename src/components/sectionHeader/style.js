const { default: styled } = require("styled-components");
export const SectionHeaderWrap = styled.div`
  .title {
    margin-top: 30px;
    font-size: 22px;
    font-weight: 700;
    margin-bottom: 16px;
    cursor: pointer;
  }

  .subtitle {
    font-size: 16px;
    margin-bottom: 20px;
  }
`