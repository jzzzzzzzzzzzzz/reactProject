const { default: styled } = require("styled-components");

export const HomeItemWrapper = styled.div`
    .content {
        width: 1032px;
        margin: 0 auto;
    }
    .content .itemBox {
        display: flex;
        flex-wrap: wrap;
        margin: 0px -8px;
}
`